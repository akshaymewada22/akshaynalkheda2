import React from "react";
import { Box, Typography } from "@mui/material";

const Gallery = () => (
  <Box id="gallery" sx={{ p: 3 }}>
    <Typography variant="h4" color="#FF4F0F">Gallery</Typography>
    <Typography>Gallery images coming soon.</Typography>
  </Box>
);

export default Gallery;
